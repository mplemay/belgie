# simple example

The smallest copyable belgie example. It demonstrates:

- async `Runtime` with a TypeScript script loaded from disk
- Python ↔ JavaScript JSON round-tripping through the `run` export

Use this example as the baseline reference when another repository needs its first belgie integration.

## Run

```bash
uv sync
uv run main
```
