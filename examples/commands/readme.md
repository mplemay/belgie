# command example

Demonstrates installing an inline npm package dependency with `Environment` and running its binary
through `Runtime` and `Command`. Command arguments are passed directly without shell parsing or an
external Deno executable.

## Run

```bash
uv sync
uv run main
```
