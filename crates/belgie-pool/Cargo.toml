[package]
name = "belgie-pool"
version = "0.1.0"
edition = "2024"

[dependencies]
belgie-protocol = { path = "../belgie-protocol" }
serde_json = "1.0.151"
tempfile = "3.27.0"
tokio = { version = "1.53.1", features = ["io-util", "process", "rt", "sync", "time"] }
