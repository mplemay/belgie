[package]
name = "belgie-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
belgie-core = { path = "../belgie-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.151"
