[project]
name = "trace"
version = "0.1.0"
requires-python = ">=3.12,<3.15"
dependencies = [
    "alchemy",
    "auth",
    "fastapi>=0.100",
    "httpx>=0.24",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]

[tool.uv.sources]
alchemy = { workspace = true }
auth = { workspace = true }
